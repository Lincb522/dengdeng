# 产品边界

DengDeng AI 是可自托管的多模型 API 中转与运营系统。它统一管理上游凭证、对外密钥、模型目录、计费和运行状态，不提供模型能力本身。

## 使用角色

| 角色 | 可完成的工作 |
| --- | --- |
| 访客 | 查看首页、公开模型广场、法律文档；在开放注册时创建账户 |
| 用户 | 管理自己的 API 密钥、分组路由、用量、余额、充值、兑换码、推广关系、账户安全和能力库 |
| 管理员 | 管理用户、分组、账号、代理、模型、价格、支付、推广分账、监控、错误、告警、备份、更新和系统策略 |
| 运维人员 | 管理进程配置、数据库、HTTPS、备份仓库、恢复密钥、更新器和主机权限 |

管理员权限不能替代主机权限。网页不能读取 `JWT_SECRET`、`ENCRYPTION_KEY`、数据库密码、备份口令或 root 独占的更新器配置。

## 核心对象

### 用户与密钥

用户持有余额、有效期套餐、按次额度、平台额度、并发和 RPM。每把 `dd-` 密钥可以选择一个或多个分组，并进一步限制总额度、日额度、并发、RPM、有效期、IP 和默认思考强度。

密钥摘要用于鉴权；加密密文用于所属用户跨设备查看、复制和快速配置。密文是否需要二次验证由系统安全策略决定。

### 分组与账号

分组定义对外路由和计费边界，包括平台、公开状态、普通倍率、缓存倍率、服务档位、长上下文、图像倍率、模型映射和最大思考强度。

上游账号保存凭证、协议、端点、额度、代理、优先级、自定义顺序、并发和健康状态。一个账号可以加入多个同平台分组；复合分组可以容纳不同平台的兼容账号。

### 模型与价格

模型目录描述对外模型名、上游模型名、平台、上下文、最大输出和能力标签。价格规则保存文本、缓存、图像或按次单价。最终费用还受用户、分组、服务档位、长上下文和思考强度倍率影响。

模型目录不是上游能力的替代品。API Key 账号的 `/v1/models` 以真实上游发现结果为准；OAuth 等无法稳定发现模型的账号才使用本地目录。

### 请求与账本

每个请求保留用户、密钥、分组、账号、模型、入口、Token、缓存、图像、耗时、IP、地区、状态和计费依据。余额变动、订单、退款、兑换码和推广提现使用各自的持久化账本，不从统计窗口反推余额。

### 能力库与图像工作台

提示词、规则和 Skill 由管理员上架，用户选择后按聊天、图像、视频、音频或全局范围应用。图像工作台使用同一账户和能力库，画布、资产与历史记录保存在浏览器本地；生成请求仍走站点 API 和正常计费链路。

## 产品约束

- 不出售、共享或自动获取第三方模型账号。
- 不保证任何订阅账号可以被转成公共 API 使用；是否允许由相应平台条款和凭证能力决定。
- 不把接口格式兼容描述成模型能力兼容。
- 不把渠道探测结果当作持续可用保证。真实请求、上游限流和账户策略可能在探测后变化。
- 不把充值余额、推广现金佣金和支付商户资金混为同一账户。
- 不用管理端网页管理主机根密钥、TLS 私钥或备份解密口令。
- 当前网关运行时并发状态以单实例为基准；多实例需要共享调度与限流设施。

## 界面规则

- 任务和状态优先，不用宣传文案填充空白。
- 金额、倍率、额度、时间范围和统计口径必须明确显示。
- 桌面端保留高密度信息；移动端重排为可操作卡片，不依赖整页横向滚动。
- 状态不能只靠颜色表达；错误必须包含失败对象、已知原因和可执行动作。
- 密钥、凭证、支付秘密和用户数据不得出现在无关页面、前端日志或错误详情。
- 首页、登录、模型广场、工作台和运营控制台可以有不同的信息密度，但使用同一术语和账户状态。

## 成功标准

一次可验证的完整接入应满足：

1. 用户创建密钥并选择可用分组；
2. `/v1/models` 返回该密钥实际可调用的模型；
3. 非流式和流式请求均能收到有效内容；
4. 用量明细记录正确的路由、Token、缓存、耗时和费用；
5. 用户余额或额度按同一账本结算；
6. 上游失败时给出可关联的错误，不返回空白成功响应；
7. 管理员能在运行监控、账号状态和错误中心复核本次请求。
