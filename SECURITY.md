# 安全说明

安全修复以当前 `main` 分支和最新发布版本为准。旧版本可能缺少后续的凭据脱敏、错误分类、注册风控和支付幂等修复。

## 报告漏洞

不要在公开 Issue、讨论区、截图或聊天记录中提交可用密钥、完整请求体、数据库、Cookie、OAuth Token、支付配置或用户数据。

私下联系维护者时提供：

1. 受影响版本、提交号和部署方式；
2. 最小复现步骤；
3. 预期结果与实际结果；
4. 影响范围；
5. 已完成的凭据轮换或隔离措施。

如果材料中曾包含真实凭据，先撤销或轮换，再发送已经脱敏的复现。维护者确认问题、修复并完成必要部署后，再决定公开时间和内容。

## 敏感数据

| 数据 | 存储与访问边界 |
| --- | --- |
| 用户密码 | 只保存密码哈希 |
| `dd-` API 密钥 | SHA-256 摘要用于鉴权；可恢复副本使用字段加密，只对所属用户开放 |
| 上游 API Key / OAuth / Agent Identity | 字段加密；管理接口不回显完整值 |
| SMTP、Turnstile、用户 OAuth Secret | 数据库字段加密，只返回“已配置”状态 |
| 支付与提现秘密 | 数据库字段加密，只在渠道调用边界解密 |
| JWT | 由 `JWT_SECRET` 签名；用户状态和会话版本仍从数据库检查 |
| 数据库与备份 | 包含用户、账务、用量和密文，整体按敏感数据处理 |

字段加密使用 `ENCRYPTION_KEY`。生产环境必须单独设置；留空时程序从 `JWT_SECRET` 派生并记录警告。直接更换 `ENCRYPTION_KEY` 会让已有密文无法解密，轮换必须包含数据迁移和可验证回滚。

不要把以下文件交给不受信任的构建、分析或备份流程：

```text
.env
config.yaml
*.db
*.key / *.pem / *.p12 / *.pfx
TLS 私钥
SSH deploy key
GPG 备份口令
```

## 网络边界

- 应用进程监听回环或私网，由 HTTPS 反向代理提供公网入口。
- 只把实际反向代理 IP/CIDR 加入可信代理。任意信任转发头会破坏注册限流、IP 规则、审计和地区统计。
- Nginx 示例将公共 API 请求体限制为 65 MiB，应用限制为 64 MiB；控制台 API 限制为 1 MiB。
- SSE 必须关闭代理缓冲并设置足够读写超时。
- 上游代理支持 HTTP、HTTPS 和 SOCKS5。代理能看到目标地址和经其转发的数据，必须由运营方控制或明确信任。
- 公共 CORS 只解决浏览器访问控制，不替代 API 密钥鉴权。

前端响应设置 CSP、禁止 frame、`nosniff`、Referrer Policy 和权限策略。新增外部脚本、Frame、图片或请求源时必须按最小范围更新 CSP，不能用 `unsafe-inline` 脚本或全域通配符解决报错。

## 账户与权限

- 首次启动如果 `ADMIN_PASSWORD` 为空，会生成随机管理员密码并打印一次启动日志。生产环境应显式设置强密码，并在首次登录后确认凭据来源。
- 管理员和普通用户共用登录入口，角色由数据库和服务端中间件判断，不能依赖前端菜单隐藏。
- 改密码、禁用账户或更改会话版本后，旧 JWT 会失效。
- TOTP、step-up 和会话绑定按实际运维流程配置；关闭某个全局开关不等于删除所有历史绑定。
- 密钥查看与轮换只能作用于当前用户自己的密钥。管理员用户列表不返回其他用户密钥原文。
- 用户 OAuth state、PKCE verifier、nonce 和交换码是一次性的，使用后删除。

注册保护应至少覆盖邮箱验证、单 IP、网段、邮箱域名、初始赠送冷却和自动封禁。初始余额大于零时先验证风控规则；已有用户余额不会因修改注册默认值自动变化。

## API 与上游

- 公共请求按用户、密钥和账号三个层级检查并发与额度。
- IP 白名单/黑名单使用服务端解析后的真实来源 IP。
- API Key 账号的模型列表以真实上游发现为准，不用本站模型目录假装上游支持。
- 上游错误按请求级、模型级和账号级隔离，避免一个坏请求封禁全部账号，也避免失效账号继续高频轮询。
- HTTP 200 中的错误对象、SSE 首个输出前的错误和空流会被预检；开始输出后不进行跨账号拼接。
- 错误中心、运行日志和下游错误必须清理 Token、私钥、Cookie、完整 `AgentAssertion` 和不必要的请求正文。
- Agent Identity 私钥和 Task 只在签名边界使用；Task 只在明确的失效 `401` 后轮换。

第三方兼容上游不受本项目控制。配置前检查其 TLS、隐私政策、日志保留、模型清单和额度接口，不要把未知代理当作可信凭据存储。

## 支付与资金

- 商户私钥、API v3 Key、Webhook Secret 和 OAuth Secret 不进入浏览器。
- 支付回调必须验证签名、商户、金额、币种和订单号；前端“支付成功”页面不能直接增加余额。
- 回调、主动查询和人工核验必须通过同一幂等订单状态机结算。
- 充值余额、支付商户资金和推广现金佣金是三个不同账务边界。
- 提现结果不确定时只查询原商户单号，不能换单重试。
- 生产渠道启用前使用渠道沙箱或最小金额验证下单、回调、查询、重复回调、退款和对账。

支付和推广分账涉及商户资质、身份核验、税务与当地法规。系统的签名和账本实现不代表运营主体已经满足这些要求。

## 备份与更新

- SQLite 快照、全栈快照和 GitHub 加密分片都属于敏感资产。
- 全栈备份会移除支付渠道、商户绑定和提现账户，但仍包含业务数据及加密上游凭证。
- GitHub 备份只使用私有仓库、仓库专用 deploy key、固定 known_hosts 和独立 GPG 口令。
- 解密口令必须离线保存；只保存密文而丢失口令等同于没有备份。
- 恢复验证写入新文件或目录，确认完整性后才由运维人员替换生产数据。
- PostgreSQL 不使用应用内置 SQLite 备份，应由数据库平台执行备份和恢复。
- 更新器仓库和分支由 root 独占配置决定。网页不能传入命令、仓库地址或 Git 凭据。
- 更新前创建数据库快照，构建成功后再切换二进制，健康检查失败自动恢复旧二进制；数据库迁移的回滚仍需单独评估。

## 生产检查

- [ ] 只部署最新已审核提交或带摘要的容器镜像
- [ ] `JWT_SECRET` 与 `ENCRYPTION_KEY` 独立且不在仓库、日志和截图中
- [ ] 管理员密码唯一，TOTP/step-up 策略已实际测试
- [ ] 应用端口不直接暴露公网，HTTPS 与证书续期正常
- [ ] `SITE_PUBLIC_URL` 与 OAuth、支付、提现回调一致
- [ ] 可信代理只包含真实代理，真实 IP 已用请求验证
- [ ] 注册赠送与防滥用规则已用多个来源场景验证
- [ ] 上游凭证权限最小化，停用账号不会继续被调度
- [ ] 支付回调、重复通知、主动查询、退款和记账本已对账
- [ ] 自动备份、异地备份和恢复都成功运行过
- [ ] `/health`、磁盘、数据库、证书、备份失败和错误率有外部监控

## 不作保证的边界

本项目不能保证第三方模型、订阅账号、代理、支付渠道或 OAuth 提供商持续可用，也不能替代上游授权和合规审查。探测成功只代表当时的测试路径可用；真实请求仍可能因模型权限、内容、地区、速率或账户策略失败。
