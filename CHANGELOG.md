# 更新记录

本文件记录 DengDeng AI 的重要功能、兼容性修复与运维变更。

## 未发布

### 上游账号多分组

- 上游账号从单一 `group_id` 升级为同平台多分组关系；旧账号在启动迁移时自动回填原分组，旧字段继续镜像首选分组以支持滚动升级和回滚。
- 调度器可从任一绑定分组选中共享账号，并在所有分组之间共享同一并发计数、冷却状态、会话亲和与模型失败状态，避免重复挂载后绕过账号并发上限。
- 添加账号支持“添加新账号”和“使用已有账号”两种模式；已有账号按 OpenAI、Claude、Gemini、Grok 分类列出，可直接增加、移除或迁移同平台分组而不复制凭据。
- 新建、编辑、OAuth 登录和 JSON 导入均支持多选分组；账号弹窗可直接创建当前平台分组并立即选中，无需离开当前流程。
- 账号列表、卡片详情、运行监控、分组筛选、分组账号统计和告警规则读取完整多分组关系；删除分组时保留仍属于其他分组的共享账号。
- 新增旧账号关系回填、账号创建与迁移、跨平台拒绝、次级分组筛选和跨分组全局并发回归测试。

### Claude 上游账号登录

- 上游账号页新增独立“登录 Claude”入口，可直接选择已启用的 Claude 分组并发起订阅账号授权。
- 添加账号时明确选择 OpenAI、Claude、Gemini 或 Grok 平台，并只列出该平台的分组；订阅登录入口可在 OpenAI 与 Claude 之间切换。
- Claude OAuth 更新为固定授权回调与授权码粘贴流程，兼容授权页返回的 `授权码#state`，并在服务端校验一次性 state 与 PKCE verifier。
- Claude 订阅授权切换到当前 Claude Code 使用的 `claude.com/cai` 入口和 Scope，修复旧授权页提示“提交的信息有误”；PKCE 参数长度与排列同步官方客户端。
- Claude Token 交换和续期改用当前 JSON 协议，补齐官方 Scope、客户端请求头、Refresh Token 轮换及账号邮箱、账号 ID 自动保存。
- 浏览器授权失败时仍可手动录入 Token 或导入 JSON；授权码输入区在移动端改为单列，不产生横向溢出。
- 新增 Claude 授权、账号落库和自动续期回归测试，同时保留 OpenAI OAuth 原有回调行为。

### 项目文档、许可证与站点协议

- README 按完整项目手册重新编排，补齐能力清单、公共端点、请求链路、首次配置、客户端示例、计费倍率、生产部署、安全检查、开发测试和常见故障排查。
- 新增 GNU LGPL v3.0 或更高版本许可证与第三方通知，明确源码授权、商标与站点运营边界，并集中致谢 Sub2API、CLIProxyAPI、New API、One API、CCSwitch 和 Aside Music 等参考项目。
- 站内默认法律文档扩展为用户协议、隐私政策、可接受使用政策、服务特定条款、免责声明和开源软件说明六份完整正文，覆盖账户密钥、支付计费、数据处理、上游风险、模型输出和开源软件归属。
- 法律页面改为安全的结构化纯文本渲染，自动识别章节标题并清除 Markdown 标题标记，不再把 `#` 或 `##` 原样显示给用户，也不会渲染管理员输入的 HTML。
- 完全未修改的旧版默认协议会按内容修订号自动升级；管理员自行编辑过的协议保持原样，不会被版本更新覆盖。
- 协议管理说明和编辑提示同步更新，默认协议日期更新为 2026-07-26；Docker Compose 补传 `SITE_PUBLIC_URL` 并统一默认站点名称。

### 弹窗滚动与紧凑布局

- API 密钥设置中的分组、配额与流量改为带当前值摘要的折叠区，默认状态在桌面和移动端均无需滚动即可完成常用修改。
- 新建密钥的分组选择限制在独立内部滚动区，避免分组数量增加后撑高整个弹窗。
- 密钥快速配置改为密钥、模型、客户端三步单页切换；Codex 与 Claude 的多配置文件改为标签切换，一次只展示一个文件。
- 长配置只在代码预览内部滚动，弹窗外层在 390、768、1024 和 1280px 宽度下保持无横向溢出，并尽量避免纵向滚动。

### 弹窗系统与表单体验

- 新增统一 `AppModal` 基础组件，集中处理遮罩、固定标题与操作栏、内容区滚动、ESC、焦点循环与归还、页面滚动锁及无障碍属性。
- 分组、API 密钥、密钥设置和上游账号弹窗重新组织信息层级；缓存倍率、网络安全、手动 OAuth Token 与高级连接设置改为按需展开，已有自定义配置在编辑时自动展开。
- 密钥快速配置收敛为确认密钥、验证模型、复制客户端配置三步流程；10 个客户端下载入口移入折叠区，客户端标签与代码预览拥有独立滚动区域。
- 新建密钥成功状态改为一次性密钥专用界面，突出复制、快速配置与只显示一次提示；保存和创建操作新增防重复提交状态。
- 桌面弹窗宽度统一限制为 500–580px，640px 以下使用底部弹层并适配安全区域；旧的全局移动端弹窗规则改为明确的兼容类，浅色、深色及 1440/1024/768/390px 均无页面级横向溢出。

### 系统设置与站点定制

- 系统设置重构为站点信息、协议与免责、功能开关、安全与认证、用户默认值、网关与调度、邮件、图像存储、业务设置、运行与审计 10 个分区；设置导航在移动端改为横向标签，不再把页面拉成长列。
- 站点名称、Logo、说明、联系方式、文档地址、公开主页内容、自定义菜单和自定义 API 端点保存后立即应用；后台模式会真实关闭邮箱注册和第三方首次注册，而不再只改变主页文案。
- 新增全站默认分页数量和可选分页规格；上游账号、普通用户用量和全站用量已读取统一默认值。
- 功能开关真实联动渠道探测、模型广场、推广返利、普通用户错误明细、TOTP 与密钥多分组；关闭入口的同时停止对应服务端行为。
- 风控中心新增“拦截”与“仅记录”模式，可配置最多 200 条不区分大小写的正文短语；命中日志只记录规则哈希、请求 ID、用户和密钥，不保存完整正文。
- 修复历史设置中的数组为 `null` 时导致侧边栏和系统设置白屏；服务端输出与前端状态均统一归一为空数组或空映射。

### 注册、邮件与第三方登录

- 邮箱注册支持按开关强制验证码，新增找回密码验证码与重置接口；Turnstile 同时保护登录、注册、验证码、密码重置和 OAuth 登录开始。
- SMTP 主机、端口、账号、密码、发件人和 TLS 可在管理端热更新，密码使用 AES-256-GCM 单独加密保存；新增测试邮件接口和统一暖色品牌模板。
- 新增低余额、分组订阅到期和上游额度告警邮件；使用同日标记避免重复通知。
- 新增 LinuxDO、钉钉、微信、标准 OIDC、GitHub 和 Google 用户登录，支持 state、PKCE、Nonce、一次性登录码与已绑定用户 TOTP。
- OIDC / Google 的 ID Token 现在从 JWKS 读取 RSA 或 EC 公钥，严格校验签名算法、Issuer、Audience、过期时间、时钟偏差和 Nonce；Google 预置官方 Issuer、Discovery 与 JWKS 地址。
- OAuth state、PKCE verifier、Nonce 和一次性登录结果迁移到共享数据库，支持多实例回调和服务重启；凭据消费后立即删除，并由到期清理回收残留记录。
- OAuth 来源支持独立余额、并发、RPM、分组订阅和 OpenAI / Anthropic / Gemini / Grok 日周月额度；注册发放与首次绑定追加发放分别控制，首次绑定使用唯一记录保证不会重复到账。
- 第三方登录长表单在移动端改为默认折叠的小卡片；每个来源可单独展开身份端点、严格校验和来源额度配置。

### 账户安全与审计

- 敏感设置、密钥轮换、备份创建/下载、图像存储、支付和分账配置可启用 15 分钟 step-up TOTP；开启前要求管理员本人已经绑定验证器。
- 控制台会话可选择绑定客户端 IP 与 User-Agent；关闭时不再强制旧的 UA 指纹，避免移动网络正常切换导致误退出。
- 操作日志按设置的保留天数每 12 小时清理；真实客户端 IP 继续统一使用受信代理与自定义请求头策略。
- SMTP 密码、Turnstile Secret 和六种 OAuth Client Secret 统一加密存储；普通设置与秘密在一个事务内保存，任何一步失败都不会留下半更新状态。

### 网关高级策略

- 401/403、429、529、普通 5xx 和网络错误分别拥有独立冷却时间；529 过载不再与其他服务端错误共用恢复窗口。
- 新增 OAuth 客户端指纹统一开关、Codex Responses metadata 透传、可编辑 Claude System 身份块、Anthropic 1 小时 TTL 和消息缓存断点重写，保存后对新请求即时生效。
- 消息缓存重写会先清理历史消息中的旧断点，再在最后一条消息和倒数第二条用户消息放置稳定的 ephemeral 断点，改善多轮对话缓存命中。
- 新增 Claude Code 与 Codex CLI 独立版本门禁；支持最低/最高版本、Codex UA/Originator 黑白名单和 app-server 放行开关，不符合规则的请求在进入账号调度前拒绝。
- Codex OAuth User-Agent 可独立配置；关闭指纹统一后继续透传真实客户端 UA、Version、Originator 与 Stainless 信号。
- OpenAI、Claude OAuth 的 System/缓存改写均保留客户端原始内容，不会重复注入已有身份块；Responses metadata 默认仍按兼容策略剥离。

### 界面验收与回归

- 公开主页在 390px 下保持品牌名、说明和入口单行完整显示，不再裁掉中文站点名；登录、找回密码、设置页和长 OAuth 表单均无页面级横向滚动。
- 浅色与深色模式完成桌面和 390px 浏览器验收；移动端侧栏、设置卡片、输入框、开关和主按钮保持足够文字对比度。
- 新增客户端版本门禁、OIDC JWKS/Nonce 和跨实例 OAuth 一次性状态测试；Impeccable 确定性界面扫描无新增问题。

### 用量明细与版本日志

- 用量明细将分组、模型和入站端点合并为“分组 / 模型”字段；表格默认双行显示完整分组与模型，悬浮或点击后查看端点、协议、平台、思考强度及上游路由详情。
- 用量时间改为日期与具体时间双排显示，在减少横向宽度的同时保留秒级精度。
- 上游账号列表由 11 列收敛为账号路由、额度用量、可用度、调度、最近使用和操作 6 列；Base URL、凭据、代理、额度窗口及上游标识改由悬浮或点击详情卡展示。
- 管理员“全站用量”移入运行中心；普通账号新增明确的“我的用量”入口，查询与导出均由服务端强制限定为当前用户。
- 入站端点改为直接显示真实请求路径，不再用 Responses、Chat 等类别名称替代实际入口。
- 旧用量记录不再显示含糊的“历史”计费模式；服务端根据账单快照、请求结果、用户角色和实际用量归类为按量、管理端或未计费。
- 版本更新新增持久化历史，保留最近 50 次发布与回滚记录，可展开查看每个版本的完整提交说明。
- 输入、输出、缓存读取和缓存创建合并为紧凑 Token 汇总，悬浮卡展示完整拆分、缓存 TTL 与总量。
- 首字耗时和总耗时合并为响应耗时摘要，按快、一般、较慢分色；详情展示排队、路由、上游、总耗时及尝试次数。
- 用量记录新增真实计费模式快照，区分按量、按次、按日、管理端和未计费请求；旧记录按可验证的请求结果、用户角色与用量信息兼容展示。
- 用量明细新增入站端点，明确请求来自 Responses、Chat、Messages、生图或 Gemini GenerateContent。
- OpenAI 兼容用量解析补齐缓存创建字段别名，兼容嵌套与顶层的 cache write / cache creation 数据。
- 版本更新器在构建前自动读取提交标题和正文，生成包含具体变更条目的完整更新日志并保留到本次发布状态。
- 管理员用量明细的用户列改为头像、用户简称和密钥摘要；悬浮卡展示完整邮箱、用户 ID、密钥名称及密钥 ID。

### Grok 与通用凭证导入

- 通用 OAuth / API Key JSON 未声明平台时，改为采用管理员明确选择的目标分组，不再一律误判成 OpenAI。
- Grok 额度查询修正为 CLI Proxy 的 `/v1/billing` 与 `/v1/billing?format=credits`，不再请求不存在的根路径 `/billing`。
- Grok 免费/统一账单账号现在能识别 `{"val": ...}` 金额结构；上游未返回用量百分比时，仍会显示周重置时间与真实月额度。
- OAuth 账号导入按目标分组和上游 `account_id` 幂等更新；重复点击导入只刷新凭证，不再新建重复账号。
- 密钥快速配置阻止移动端密码管理器把登录密码填入 API Key 输入框；非 `dd-` 密钥不再发起模型检测或覆盖浏览器临时密钥。
- Grok 导入新增 `auth.x.ai`、`api.x.ai`、Grok CLI Base URL、Scope 与订阅字段识别，并保留订阅层级、权益状态及 Team 元数据。
- 明确声明平台的文件仍执行严格分组校验，不会把真正的 OpenAI 凭证写入 Grok 分组。
- 导入弹窗不再把 `tokens` 结构直接等同于 OpenAI；全量跳过时显示真实原因，不再先提示“导入完成”。
- 服务端新增不含凭证内容的导入汇总日志，后续可直接看到目标分组、成功数、更新数及跳过原因统计。

### Agent Identity 与 OpenAI 账号

- Agent Identity 按最新 Sub2API 收敛为 Codex `auth.json` 导入，不再在管理端接收 Access Token 或 Web Session；导入后只保留 Runtime、Ed25519 私钥和账户身份。
- 同时兼容 `auth_mode=agentIdentity` 和 Codex 托管模式生成的 `auth_mode=chatgpt + agent_identity`，并支持单对象、JSON 数组和 JSONL 批量导入。
- Agent Identity 按最新版 Sub2API 改为只用 ChatGPT Account ID 去重：同一 Team 重新生成 Runtime 时原位轮换，不同 Team 即使属于同一用户也保持隔离。
- 缺少 `task_id` 时由首次请求自动补注册；导入文件中并存的 OAuth Token、Refresh Token 和 ID Token 不会写入数据库。
- 导入时忽略 Agent Identity JSON 中残留的 OAuth `expires_at`，不会再把仍可签名的账号误判为过期并跳过。
- OpenAI Responses、Chat Completions、生图和账号额度查询统一使用动态 `AgentAssertion`；`task_id` 失效时自动重建一次并加密回写。
- 网关、额度刷新和账号探针共用账号级 task 锁；并发请求会在锁内重新读取已回写的 `task_id`，不会重复注册或相互覆盖。
- task 注册遇到结果不确定的网络/服务端错误不再盲目重试；只有上游明确返回 task 失效 401 时才按旧 task 精确轮换一次。
- Agent Identity 上游错误会清除 Runtime、Task、私钥与完整 `AgentAssertion` 后再写入日志或返回客户端。
- 账号健康检查会用签名后的额度请求验证 Agent Identity，而不是只检查 ChatGPT 域名能否连通，也不会再使用 OAuth 过期时间判定。
- 企业 FedRAMP 身份会在对话、Responses、生图与额度请求中实际发送 `x-openai-fedramp`，不再只保存导入标记。

### 全凭证上游额度

- OAuth 与 Agent Identity 继续读取上游订阅窗口；API Key 不再只显示本站记录，会直接查询第三方中转站的余额、密钥额度与订阅窗口。
- 自动识别 DengDeng / Sub2API `/v1/usage`、New API `/api/usage/token`、One API dashboard billing 和 credit grants 返回结构，兼容驼峰、下划线及中转原生额度单位。
- 上游账号可单独填写同域“额度查询地址”，用于接入非标准第三方中转；同源校验会阻止 API Key 被发送到其他域名。
- OpenAI、Anthropic、Gemini、Grok API Key 均支持主动凭证探测；上游不开放余额时明确显示密钥可用状态，不再伪造现金余额。
- 网关会从真实对话、生图与模型请求响应中持续采集请求、Token、输入 Token、输出 Token 限额，账号卡片随实际调用更新。
- 兼容额度接口支持 Bearer 与原生平台认证头，Base URL 可填写主机地址或带 `/v1`、`/v1beta` 的 SDK 地址。
- `limit=0` 按无限额度处理，不再错误显示为“剩余 0”或触发账号额度耗尽。

### 调度、流式响应与计费

- 调度器会记录分组内账号被排除的实际原因，包括停用、冷却、额度耗尽、模型冷却、并发已满和本请求已尝试；运行监控直接展示最新 503 诊断。
- OAuth / Agent Identity 流在实际输出前预检 HTTP 200 中的错误事件、空流和缺失终止事件；仅在尚未下发内容时安全切换账号，避免重放工具调用。
- SSE 解析兼容 `event:name` / `data:{...}` 紧凑格式；本地余额和密钥额度错误改为 OpenAI / Anthropic 客户端可识别的标准结构。

### 思考强度策略

- OpenAI / Grok 分组新增思考强度映射和最高档位。客户端显式选择、密钥默认值、分组映射与分组上限按固定顺序生效。
- OpenAI Responses、Chat Completions 和 Claude Code 转 Responses 共用同一策略，用量日志与账单按最终发给上游的档位计算。

### 管理安全与真实 IP

- 账户设置新增 RFC 6238 TOTP 绑定、二维码和关闭流程；开启后管理员操作要求当前会话已完成验证器校验。
- 新签发的控制台 JWT 绑定浏览器 User-Agent 指纹，在其他客户端重放时要求重新登录；开关 TOTP 会同时作废旧会话。
- 系统设置可热更新受信反向代理 IP/CIDR 和真实客户端 IP 请求头，避免未受信来源伪造 `X-Forwarded-For`。

### 异步生图与对象存储

- 新增 S3 兼容图像存储设置，支持 AWS S3、Cloudflare R2、MinIO 及兼容服务；密钥加密保存，管理端可测试 Bucket 连通性。
- 新增 `POST /v1/images/generations/async` 和 `GET /v1/images/tasks/:task_id`。任务结果会先上传对象存储，数据库只留短链接与状态；任务轮询不受余额耗尽影响，且严格校验密钥所有权。

### 密钥多分组

- 快速配置将“工具下载”和“协议配置”拆分展示：Claude、Codex、Gemini、Chatbox、Cherry Studio、NextChat、Cline、Continue、OpenCode、CCSwitch 的官方下载入口始终完整显示，配置模板仍按当前分组协议筛选。
- 密钥快速配置新增 Cherry Studio、NextChat 和 Continue，并为 Claude Code、Codex CLI、Gemini CLI、Chatbox、Cline、OpenCode、CCSwitch 等每种工具补充官方下载安装入口。
- 新创建或手动补入的完整密钥改为保存在当前浏览器本机，刷新、关闭标签页或重启浏览器后仍可显示和复制；列表支持显示、隐藏、复制和清除本机密钥。
- 本机密钥会与服务端预览前后缀校验，轮换后自动丢弃旧值，避免快速配置继续使用已经失效的密钥。
- 一把 API 密钥现在可同时绑定最多 32 个开放分组，创建和编辑界面均支持多选；旧密钥会在启动迁移时自动保留原分组，不需要重新生成。
- 网关会按请求模型与协议选择匹配平台，同平台选中多个分组时可在账号池不可用、排队拥塞或可重试上游错误后切换到下一分组。
- `GET /v1/models` 会汇总密钥所选平台的模型目录，并支持 `platform` 参数按平台筛选；未绑定的平台不会被暴露。
- 快速配置新增接入平台切换，分别生成 OpenAI、Claude、Gemini 或 Grok 对应配置并读取该平台模型。
- 密钥列表完整显示全部分组且保持单行排版，移动端创建/编辑多选区域自动改为单列；浅色、深色主题均完成对比度验收。

### 备份与版本更新

- 数据库备份新增可持久化的自动调度策略，支持启停、执行间隔、保留天数和最大份数；默认每 24 小时备份一次，保留 30 天且最多 30 份。
- 新增手动执行过期清理。自动清理严格限定为系统自动生成的快照，管理员手动备份永不自动删除。
- 版本检查新增提交级更新日志，显示标题、短提交号和提交时间；更新完成后保留本次上线日志。
- 数据库备份页补充下一次执行时间、来源标记和策略状态，版本更新页补充待更新内容与空状态。
- 移动端侧栏新增可触达的关闭按钮，避免遮罩点击落到下层菜单；深色模式主按钮改为高对比配色。

### 界面修复

- 分组标签统一保持单行完整显示；移动端表格的分组字段占满记录宽度，超长名称可在字段内横向查看，不再自动换行或省略。
- 所有数据表头与数据单元格统一保持单行；移动端超长值仅在字段内部横向查看，不再把整张记录拉成长列。
- 14 天调用趋势新增 Token / 请求双轴刻度、总量与日均摘要、峰值明细和逐点键盘提示；运行监控请求走势改为成功/失败堆叠柱，并补齐请求量刻度、成功率、平均耗时、Token 与费用明细。
- 版本更新页移除仓库跟随说明，只保留版本与更新状态。
- 主操作按钮统一使用主题前景色与表面色，提升深色模式下生成推广码、新建密钥等操作的文字对比度。
- 模型广场卡片中的模型名、说明、能力、规格、价格和分组保持单行完整显示；超出卡片宽度时可在卡片内横向查看，不再自动换行或省略文字。

### 发布与部署

- 新增 GitHub Packages 容器发布流程，自动构建 Linux amd64/arm64 镜像并发布到 GHCR。
- 正式版本生成完整语义化标签和 `latest`，主分支生成 `edge`，所有构建保留 `sha-*` 标签。
- 容器包附带 SBOM、构建来源证明与 OCI 仓库元数据，支持 GitHub 供应链验证。
- Docker 构建启用 pnpm、Go 模块与编译缓存，并使用目标架构原生交叉编译，减少多架构发布时间。
- 固定 pnpm 版本，并仅允许构建前端所需的 esbuild 安装脚本，其余依赖脚本继续默认拒绝。
- Compose 默认可拉取已发布镜像，同时保留从当前源码构建的方式。

### 验证

- 后端：`go test ./...`
- 前端：`pnpm build`
- 覆盖全平台 API Key 额度/凭证探测、真实响应限额采集、密钥多分组创建与编辑、旧数据迁移、跨分组故障切换、分平台模型目录，以及浅色、深色和移动端界面验收。

## v0.1.0 — 2026-07-18

### 并发控制与账号调度

- 新增用户、API 密钥、上游账号三级并发上限。`0` 表示不限制，旧数据升级后保持原有行为。
- 用户和密钥并发槽通过一次原子判断占用，避免只拿到部分资源后形成死锁；流式响应结束、客户端取消、上游失败和重试切换都会释放槽位。
- 上游账号达到并发上限后进入有界等待队列。队列已满或等待超时时返回 `429`、`Retry-After`，防止请求无限堆积。
- 管理端系统设置新增并发等待时间和队列深度；用户、密钥、账号的新建与编辑界面均可配置并发上限。
- Sub2API JSON 导入和浏览器 OAuth 创建账号时会保留并发配置。
- 调度器在同优先级账号间综合当前负载、延迟 EWMA、错误 EWMA 和最近选择时间，优先选择更空闲、更稳定的账号。

### 请求兼容与故障切换

- OpenAI Chat Completions、Responses、Codex OAuth 和 Anthropic 转 Responses 共用请求标准化逻辑。
- 自动补齐函数工具缺失的对象参数结构，并修复对象根节点下未声明类型的 `anyOf` / `oneOf` 分支，减少 Claude Code、Codex 等客户端的 `Invalid tool parameters`。
- Codex Responses Lite 请求强制关闭并行工具调用；无工具请求会移除无效的 `parallel_tool_calls`。
- 超长 Responses 输入项 ID 会稳定缩短并保留唯一性，避免上游长度校验失败。
- 扩展可切换账号的错误范围：支持在 `404`、`405`、`413`、部分能力不匹配的 `400/422` 以及既有鉴权、限流、服务端错误后尝试下一个账号。
- 普通请求参数错误仍直接返回客户端，避免无意义重试放大流量。

### 监控与排障

- 用量记录新增排队、调度、上游耗时和实际上游尝试次数。
- 响应新增 `Server-Timing` 与 `X-DengDeng-Upstream-Attempts`，并保留原有 `X-Request-ID`。
- 新增 `X-DengDeng-Trace-ID`，便于客户端与管理端日志关联。
- 运行监控新增“等待槽位”实时指标；管理员用量表和 CSV 导出显示详细阶段耗时及重试次数。
- 失败请求也写入统一用量日志，但不会产生费用，便于定位账号池耗尽和最终上游错误。

### 前端与安全

- 修复推广中心、推广管理和兑换码页面在接口返回 `null` 时读取 `length` 导致的白屏。
- 修复侧边栏推广中心图标的无效 SVG 路径。
- 将主题初始化逻辑移至同源静态脚本，兼容严格 CSP；`script-src` 无需放开 `unsafe-inline`。
- 监控页面卸载时会清理刷新定时器，避免离开页面后继续请求。

### 数据与兼容性

- 用户、API 密钥、上游账号新增并发字段；用量日志新增四项链路指标，由服务启动时的数据库迁移自动补齐。
- 空推广码、佣金和兑换码集合统一返回 JSON 数组，前端同时保留防御性归一化。
- 架构文档补充三级并发、排队、释放和单实例状态边界。当前实现适用于单网关进程，多副本部署前需将并发计数迁移到 Redis 等共享存储。

### 验证

- 后端：`go test ./...`
- 前端：`pnpm build`
- 覆盖并发占用与释放、队列满和超时、客户端取消、账号排队、`413` 故障切换、请求标准化、运行指标、严格 CSP 和空推广数据等回归场景。
